#include "Headers/player.h"

/*PLAYER::PLAYER(Texture &image) //конструктор класса
{
    sprite.setTexture(image);
    rect = FloatRect(100,180,16,16); //две координаты, ширина, высота

    dx=dy=0.1;
    currentFrame = 0;
}*/
